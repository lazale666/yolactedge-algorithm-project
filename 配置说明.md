# 项目配置说明

本文档用于说明：如果你删除整个项目目录，然后重新从 Git 拉取本仓库，除了模型权重以外，还需要重新准备什么，哪些内容 Git 会带下来，哪些内容必须在本机重新配置。

文档重点面向当前这个 Windows 本地推理演示项目，而不是上游 Linux / Jetson / TensorRT 的完整部署路线。

## 1. 先说结论

如果你删除项目后重新 `git clone`，通常**不只是补回权重文件**。

你至少还需要重新确认或重新配置以下内容：

1. Python 运行环境
2. 虚拟环境 `.venv38`
3. PyTorch / TorchVision 依赖
4. 其他 Python 依赖
5. `pycocotools` 的本地编译安装
6. `yolact_edge` 包的本地安装
7. Visual Studio C++ 编译环境
8. 模型权重文件
9. 如果你要训练或评估，还需要数据集

如果你只是做最基础的本地图片 / 视频 / 摄像头推理演示，那么最小恢复集一般是：

1. Python 3.8
2. `.venv38`
3. `torch==1.8.1+cu111`
4. `torchvision==0.9.1+cu111`
5. 常规 Python 依赖
6. `cocoapi/PythonAPI` 安装
7. `yolact_edge` 安装
8. `weights/` 下的模型文件

## 2. Git 会带下来什么

重新从 Git 获取项目后，以下内容会直接恢复：

- 项目源码
- `terminal/` 下的 GUI 和终端脚本
- `yolact_edge/` 下的算法代码
- `cocoapi/` 下的本地源码
- 根目录和子目录中的说明文档
- 示例图片、部分展示结果素材

这些内容属于版本库中的文件，重新拉取后会自动存在。

## 3. Git 不会帮你恢复什么

以下内容不是“删掉项目再拉仓库就自动可用”的东西。

### 3.1 模型权重

`yolact_edge/weights/` 被忽略，不会随着 Git 恢复。

你需要自己重新放回，例如：

- `yolact_edge_54_800000.pth`
- `yolact_edge_resnet50_54_800000.pth`
- `yolact_edge_vid_847_50000.pth`

建议存放位置：

```text
yolact_edge\weights\
```

### 3.2 虚拟环境

`.gitignore` 已将 `yolact_edge/.venv38/` 列为忽略目录，这说明它应当被视为**本机环境**，不是项目源码的一部分。

虽然当前仓库历史中曾提交过 `yolact_edge/.venv38/` 的部分内容，但这并不意味着它可直接跨机器、跨目录复用。虚拟环境中的解释器路径和已安装包通常与本机路径绑定。

例如，当前环境配置里记录了：

```text
home = C:\Users\10460\miniconda3
version = 3.8.15
```

这意味着：

- 如果你换了电脑，基本不能直接复用
- 即使是同一台电脑，目录变化后也不建议把旧虚拟环境当成正式恢复方案
- 正确做法仍然是重新创建 `.venv38`

### 3.3 训练 / 评估数据集

以下目录也被忽略：

- `yolact_edge/data/coco/`
- `yolact_edge/data/YoutubeVIS/`
- `yolact_edge/data/FlyingChairs/`
- `yolact_edge/data/MOTS/`
- `yolact_edge/data/calib_images/`

因此：

- 仅做本地推理演示时，这些通常不是必须的
- 做训练、COCO 评估、YouTube VIS、TensorRT INT8 校准时，就必须重新准备

### 3.4 运行生成物

以下内容即使删掉，也不是必须恢复的：

- `terminal/outputs/`
- 推理导出结果
- 截图
- 录制视频
- 临时缓存

这些都是运行时生成物。

## 4. 为什么重新拉仓库后不能直接跑

很多人会误以为：

“既然代码都在 Git 里，重新拉下来应该就能运行。”

这类项目通常不是这样，原因主要有三类。

### 4.1 Python 包不是源码的一部分

像这些包都需要你重新安装：

- `torch`
- `torchvision`
- `opencv-python`
- `numpy`
- `pillow`
- `matplotlib`
- `cython`
- `GitPython`
- `termcolor`
- `tensorboard`
- `colorama`

### 4.2 本项目有本地编译步骤

本项目不是纯 Python 脚本项目。

它至少涉及两块本地编译相关内容：

1. `cocoapi/PythonAPI`
2. `yolact_edge/utils/cython_nms.pyx`

因此需要本机可用的 C++ 编译环境。

### 4.3 项目脚本依赖固定环境路径

例如 GUI 启动脚本默认调用：

```bat
yolact_edge\.venv38\Scripts\python.exe
```

这意味着如果你重建环境时不用 `.venv38` 这个名字，那么现有脚本可能就不能直接运行。

## 5. 推荐恢复环境

根据仓库现有说明，当前最推荐的本地恢复环境如下：

- 操作系统：Windows 10 / 11
- Python：`3.8`
- 推荐已验证版本：`3.8.15`
- PyTorch：`1.8.1+cu111`
- TorchVision：`0.9.1+cu111`
- GPU：NVIDIA CUDA GPU
- 首次验证：优先关闭 TensorRT

如果你的目标只是“先跑通”，不要一开始就折腾 TensorRT。

建议先使用：

```powershell
--disable_tensorrt
```

## 6. 删库后重新恢复的标准流程

下面是一套适合当前仓库的恢复顺序。

## 6.1 重新获取项目

```powershell
git clone <你的仓库地址>
cd yolactedge-algorithm-project
```

如果仓库目录名和当前不一致，后续命令里的路径需要同步调整。

## 6.2 创建虚拟环境

推荐仍然使用 `.venv38` 这个名字，因为现有脚本默认按这个目录寻找 Python。

在项目根目录执行：

```powershell
python -m venv .\yolact_edge\.venv38
```

如果你的系统没有合适的 `python` 命令，也可以直接用已安装的解释器，例如：

```powershell
C:\Users\你的用户名\miniconda3\python.exe -m venv .\yolact_edge\.venv38
```

验证：

```powershell
.\yolact_edge\.venv38\Scripts\python.exe -V
```

预期是 Python 3.8.x。

## 6.3 升级基础打包工具

```powershell
.\yolact_edge\.venv38\Scripts\python.exe -m pip install --upgrade pip setuptools wheel
```

## 6.4 安装 PyTorch 和 TorchVision

```powershell
.\yolact_edge\.venv38\Scripts\python.exe -m pip install torch==1.8.1+cu111 torchvision==0.9.1+cu111 -f https://download.pytorch.org/whl/torch_stable.html
```

验证：

```powershell
.\yolact_edge\.venv38\Scripts\python.exe -c "import torch, torchvision; print(torch.__version__); print(torchvision.__version__); print(torch.cuda.is_available())"
```

建议确认三点：

- `torch` 版本正确
- `torchvision` 版本正确
- `torch.cuda.is_available()` 返回 `True`

如果你只想先跑 CPU，也可以继续，但速度会明显慢很多。

## 6.5 安装常规 Python 依赖

```powershell
.\yolact_edge\.venv38\Scripts\python.exe -m pip install cython numpy opencv-python pillow matplotlib GitPython termcolor tensorboard colorama
```

## 6.6 准备 Visual Studio C++ 编译环境

Windows 下编译 `pycocotools` 和 `cython_nms` 时，通常需要 `cl` 命令可用。

先直接检查：

```powershell
cl
```

如果能看到 Microsoft C/C++ 编译器输出，说明环境已加载。

如果提示找不到 `cl`，需要：

1. 安装 Visual Studio 2022 Community 或 Build Tools
2. 勾选 C++ 相关组件
3. 使用 `Developer PowerShell for VS`

或者在普通 PowerShell 中手动加载开发环境，例如：

```powershell
& "E:\visual studio\Microsoft Visual Studio\18\Community\Common7\Tools\Launch-VsDevShell.ps1" -Arch amd64 -HostArch amd64
```

你机器上的实际路径可能不同，需要按实际安装位置调整。

## 6.7 安装 pycocotools

本仓库已经包含 `cocoapi/` 源码，通常不需要再去 GitHub 额外 clone。

执行：

```powershell
cd .\cocoapi\PythonAPI
..\..\yolact_edge\.venv38\Scripts\python.exe -m pip install .
cd ..\..
```

验证：

```powershell
.\yolact_edge\.venv38\Scripts\python.exe -c "from pycocotools.coco import COCO; print('COCO ok')"
.\yolact_edge\.venv38\Scripts\python.exe -c "from pycocotools.ytvos import YTVOS; print('YTVOS ok')"
```

如果这一步失败，最常见原因是：

- 没有加载 VS 编译环境
- C++ 编译工具未安装完整
- Python 版本不合适

## 6.8 安装 yolact_edge

执行：

```powershell
cd .\yolact_edge
.\.venv38\Scripts\python.exe -m pip install .
cd ..
```

验证：

```powershell
.\yolact_edge\.venv38\Scripts\python.exe -c "import yolact_edge; print('yolact_edge ok')"
```

这一步会处理项目本身的安装，并编译 `cython_nms` 相关扩展。

## 6.9 放回权重文件

将模型文件放回：

```text
yolact_edge\weights\
```

建议首次优先使用：

- `yolact_edge_54_800000.pth`

如果权重和配置不匹配，模型加载会失败，所以权重名称和使用方式要对应。

## 6.10 做一次最小验证

进入 `yolact_edge` 目录执行：

```powershell
cd .\yolact_edge
.\.venv38\Scripts\python.exe .\eval.py --disable_tensorrt --trained_model=weights\yolact_edge_54_800000.pth --score_threshold=0.3 --image=test.jpg
```

如果这一条能跑通，说明核心恢复已经基本完成。

## 7. GUI 能不能直接用

可以，但前提是以下几项已经满足：

1. `.venv38` 已重建
2. 依赖已安装
3. `yolact_edge` 已安装
4. 权重文件已放好

运行方式：

```powershell
cd .\terminal
.\run_gui.bat
```

或者从根目录直接执行：

```powershell
& ".\yolact_edge\.venv38\Scripts\python.exe" .\terminal\gui_app.py
```

如果 `run_gui.bat` 报找不到 Python，优先检查：

- `.venv38` 是否创建在 `yolact_edge\` 下
- 路径是否仍为 `yolact_edge\.venv38\Scripts\python.exe`

## 8. 哪些情况不用重新配置

以下情况通常不需要额外准备：

### 8.1 只查看源码

如果你只是：

- 阅读代码
- 看项目结构
- 查文档
- 改前端 GUI 文本

那么仅 `git clone` 就够了。

### 8.2 只看仓库里现成的示例素材

仓库中已有：

- 文档
- 示例图片
- 部分展示结果

这些不用你自己恢复。

### 8.3 不做训练、不做评估

如果你只做本地推理演示，一般暂时不需要：

- COCO 数据集
- YoutubeVIS 数据集
- FlyingChairs
- MOTS
- TensorRT 校准数据

## 9. 哪些情况必须追加配置

### 9.1 你要训练模型

除了基础环境，还需要：

- 对应数据集
- 数据集目录结构
- 更完整的 CUDA / 显存条件
- 训练权重和保存目录

### 9.2 你要做 COCO 评估

你需要重新准备：

- `data/coco/images/`
- `data/coco/annotations/`

### 9.3 你要做 YoutubeVIS

你需要重新准备：

- `data/YoutubeVIS/annotations/`
- `data/YoutubeVIS/train_all_frames/`
- `data/YoutubeVIS/valid_all_frames/`

### 9.4 你要启用 TensorRT

Windows 下这不是首选恢复路径。

建议顺序是：

1. 先用 `--disable_tensorrt` 跑通
2. 确认图片、视频、摄像头推理都正常
3. 再单独处理 TensorRT

## 10. 常见误区

### 10.1 “仓库里已经有 `.venv38`，所以不用重建”

不建议这样理解。

虚拟环境本质上是本机环境产物，不应作为可移植交付物依赖。当前仓库即使残留了历史提交内容，也不能保证直接可用。

### 10.2 “只要补回权重就能运行”

不对。

如果缺少：

- Python 依赖
- C++ 编译环境
- `pycocotools`
- `yolact_edge` 本地安装

项目通常跑不起来。

### 10.3 “Git clone 下来就等于完整部署”

不对。

Git 只恢复版本库中的文件，不恢复：

- pip 安装的包
- 本机 GPU 环境
- VS 编译器
- CUDA 可用状态
- 你本地的数据集
- 权重文件

### 10.4 “首次就应该把 TensorRT 配好”

不建议。

当前项目文档本身更推荐先走纯 PyTorch 推理链路，再考虑 TensorRT。

## 11. 推荐的最短恢复清单

如果你的目标只是删库后尽快重新跑起来，按下面做就够了：

1. 重新 `git clone`
2. 用 Python 3.8 创建 `yolact_edge\.venv38`
3. 安装 `torch==1.8.1+cu111`
4. 安装其他依赖
5. 确保 `cl` 可用
6. 安装 `cocoapi/PythonAPI`
7. 安装 `yolact_edge`
8. 放回 `weights\yolact_edge_54_800000.pth`
9. 用 `--disable_tensorrt` 跑 `eval.py`

## 12. 最后总结

删掉项目重新从 Git 获取后：

- **源码一般不用你额外恢复**
- **权重一定要自己放回**
- **虚拟环境必须视为需要重建**
- **依赖必须重新安装**
- **编译环境通常也要重新确认**
- **训练或评估数据集不会自动回来**

如果你只是想重新跑本地推理演示，最核心的是：

1. Python 3.8
2. `.venv38`
3. PyTorch / TorchVision
4. 常规依赖
5. `pycocotools`
6. `yolact_edge`
7. 权重

把这几项补齐，项目就能恢复到可运行状态。
