# 项目处理流程说明

本文说明本项目从“用户选择图片/视频/摄像头”到“YOLACT Edge 模型输出实例分割结果”的完整处理流程。文中会同时点明每一步用到的函数和所在文件。

## 1. 项目整体分层

本项目可以分成三层：

```text
用户界面层
  terminal/gui_app.py
  terminal/visual_terminal.py

统一推理后端层
  terminal/inference_backend.py

YOLACT Edge 算法层
  yolact_edge/yolact_edge/yolact.py
  yolact_edge/yolact_edge/utils/augmentations.py
  yolact_edge/yolact_edge/layers/functions/detection.py
  yolact_edge/yolact_edge/layers/output_utils.py
```

各层职责如下：

| 层级 | 主要文件 | 作用 |
| --- | --- | --- |
| GUI 前端 | `terminal/gui_app.py` | 提供桌面窗口、按钮、文件选择、图片/视频预览、截图、录制、导出 |
| 命令行前端 | `terminal/visual_terminal.py` | 提供命令行参数和 OpenCV 预览窗口 |
| 推理后端 | `terminal/inference_backend.py` | 加载模型、接收图像、预处理、推理、后处理、绘制结果 |
| 算法模型 | `yolact_edge/yolact_edge/yolact.py` | 定义 YOLACT Edge 网络结构和前向推理流程 |
| 图像预处理 | `yolact_edge/yolact_edge/utils/augmentations.py` | 把图像变成模型能接收的格式 |
| 检测后处理 | `yolact_edge/yolact_edge/layers/functions/detection.py` | 对模型预测结果进行解码、过滤和 NMS |
| mask 后处理 | `yolact_edge/yolact_edge/layers/output_utils.py` | 把 mask 系数组合成最终实例分割 mask |

整体数据流如下：

```text
图片文件 / 视频帧 / 摄像头帧
  ↓
OpenCV 读取为 numpy.ndarray 图像矩阵
  ↓
torch.from_numpy 把 OpenCV 图像转成 PyTorch Tensor
  ↓
FastBaseTransform 做缩放、归一化、BGR 转 RGB、维度调整
  ↓
Yolact 模型推理
  ↓
Detect 做边框解码、置信度过滤、NMS
  ↓
postprocess 生成最终类别、分数、边框、mask
  ↓
OpenCV 绘制 mask、边框、类别文字
  ↓
GUI / OpenCV 窗口显示，或保存为图片/视频
```

## 2. 先解释几个专有名词

### 2.1 OpenCV

OpenCV 是一个常用的计算机视觉库。本项目用它做这些事：

- 读取图片：`cv2.imread`
- 读取视频和摄像头：`cv2.VideoCapture`
- 保存图片：`cv2.imwrite`
- 保存视频：`cv2.VideoWriter`
- 改变图片大小：`cv2.resize`
- 画矩形框：`cv2.rectangle`
- 写类别文字：`cv2.putText`
- 显示命令行窗口画面：`cv2.imshow`

OpenCV 读出来的图片通常是一个 `numpy.ndarray`，可以理解成一个三维数字表格：

```text
高度 × 宽度 × 颜色通道
```

例如一张 720p 彩色图片，形状可能是：

```text
(720, 1280, 3)
```

最后的 `3` 表示三个颜色通道。

### 2.2 BGR 和 RGB

普通人一般说图片颜色是 RGB：

```text
R = Red 红色
G = Green 绿色
B = Blue 蓝色
```

但 OpenCV 默认使用 BGR 顺序：

```text
B = Blue 蓝色
G = Green 绿色
R = Red 红色
```

所以项目里经常会看到：

```python
cv2.cvtColor(original_bgr, cv2.COLOR_BGR2RGB)
```

意思是把 OpenCV 的 BGR 图像转成 RGB 图像，避免显示时颜色错乱。

### 2.3 numpy.ndarray

`numpy.ndarray` 是 NumPy 库中的数组对象。OpenCV 读出来的图片就是这种数组。

在本项目中，`frame_bgr`、`image`、`working_frame` 这些变量通常都是 `numpy.ndarray`。

例如：

```python
image = cv2.imread(str(image_path))
```

这里的 `image` 就是一张 OpenCV 图像，也就是 `numpy.ndarray`。

### 2.4 PyTorch Tensor

PyTorch Tensor 是 PyTorch 深度学习框架中的数据对象。模型不能直接处理 OpenCV 的 `numpy.ndarray`，需要先转成 Tensor。

本项目中的转换代码在 `terminal/inference_backend.py` 的 `YolactInferenceBackend.predict` 中：

```python
frame_tensor = torch.from_numpy(working_frame).to(self.device).float()
```

这句话的意思是：

```text
把 OpenCV 图像 working_frame
  ↓
转成 PyTorch Tensor
  ↓
移动到 CPU 或 GPU
  ↓
转换为 float 类型
```

### 2.5 device、CPU、CUDA、GPU

`device` 表示模型运行在哪个设备上。

在 `terminal/inference_backend.py` 中：

```python
self.device = torch.device(
    "cpu" if self.runtime_args.cpu or not torch.cuda.is_available() else "cuda"
)
```

意思是：

- 如果用户指定 CPU，或者电脑没有可用 CUDA，就用 CPU。
- 否则使用 CUDA，也就是 NVIDIA GPU。

CUDA 是 NVIDIA 显卡用于加速深度学习计算的技术。

### 2.6 batch

深度学习模型通常一次接收一批图片，而不是一张图片。因此即使只处理一张图，也要在最前面增加一个“批次维度”。

本项目中：

```python
batch = self.transform(frame_tensor.unsqueeze(0))
```

`unsqueeze(0)` 的意思是在第 0 维增加一个维度。

如果原图形状是：

```text
高度 × 宽度 × 颜色通道
```

加上 batch 维度后变成：

```text
1 × 高度 × 宽度 × 颜色通道
```

这个 `1` 表示当前这一批只有一张图。

### 2.7 transform

`transform` 是“预处理”的意思。模型不能直接吃原始图片，需要先统一尺寸、颜色顺序、数值范围和维度顺序。

本项目使用：

```python
self.transform = FastBaseTransform()
```

对应文件：

```text
yolact_edge/yolact_edge/utils/augmentations.py
```

核心类：

```python
class FastBaseTransform(torch.nn.Module)
```

它主要做这些事：

```text
把图像从 [N, H, W, C] 转成 [N, C, H, W]
resize 到模型配置尺寸
减均值、除标准差进行归一化
把 BGR 通道顺序转成 RGB
```

### 2.8 NMS

NMS 是 Non-Maximum Suppression，中文常译为“非极大值抑制”。

模型可能对同一个物体预测出多个很接近的框。NMS 的作用是：

```text
保留分数最高的框
删除和它重叠太多的其他框
```

这样最终每个物体尽量只保留一个检测框。

本项目中 NMS 逻辑在：

```text
yolact_edge/yolact_edge/layers/functions/detection.py
```

核心类：

```python
class Detect
```

相关函数：

```python
detect
fast_nms
traditional_nms
```

### 2.9 mask

mask 是分割掩码。它可以理解成一张黑白图：

```text
白色区域：属于这个物体
黑色区域：不属于这个物体
```

实例分割的目标不是只画框，而是要给每个物体画出轮廓区域。这个区域就是 mask。

### 2.10 prototype mask 和 mask 系数

YOLACT 的核心思想是：

```text
先生成一组全图共享的 prototype mask
再为每个物体预测一组系数
用 prototype mask 和系数组合出每个物体自己的 mask
```

这样比“每个物体单独生成一张 mask”更快，适合实时分割。

组合过程在：

```text
yolact_edge/yolact_edge/layers/output_utils.py
```

核心函数：

```python
postprocess
```

## 3. GUI 前端处理流程

GUI 前端文件：

```text
terminal/gui_app.py
```

主类：

```python
class VisualSegApp(tk.Tk)
```

这个类继承自 `tk.Tk`，表示它本身就是一个桌面窗口。

### 3.1 GUI 初始化

入口函数：

```python
def main() -> None:
    DEFAULT_OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
    app = VisualSegApp()
    app.mainloop()
```

流程如下：

```text
创建 outputs 输出目录
创建 VisualSegApp 窗口
进入 Tkinter 主循环
```

`app.mainloop()` 是 GUI 程序的事件循环。它会持续等待用户点击按钮、选择文件、关闭窗口等操作。

### 3.2 GUI 界面布局

函数：

```python
VisualSegApp._build_layout
```

所在文件：

```text
terminal/gui_app.py
```

它负责创建界面元素，包括：

- 权重路径输入框
- “浏览”按钮
- “加载模型”按钮
- 置信度阈值滑块
- “禁用 TensorRT”复选框
- “导出拼接图”复选框
- 预览边长下拉框
- 摄像头索引输入框
- “打开图片”按钮
- “保存图片”按钮
- “打开视频”按钮
- “播放视频”按钮
- “启动摄像头”按钮
- “停止预览”按钮
- “导出视频”按钮
- “截图当前帧”按钮
- “开始录制”按钮
- 左侧原始画面显示区
- 右侧实例分割结果显示区
- 底部状态栏

按钮和函数的关系如下：

| 按钮 | 绑定函数 | 作用 |
| --- | --- | --- |
| 浏览 | `choose_weights` | 选择 `.pth` 权重文件 |
| 加载模型 | `load_model` | 创建并初始化推理后端 |
| 打开图片 | `open_image` | 选择图片并做一次推理 |
| 保存图片 | `save_current_image` | 保存当前分割结果 |
| 打开视频 | `open_video` | 选择视频文件 |
| 播放视频 | `play_video` | 逐帧推理并预览视频 |
| 启动摄像头 | `start_camera` | 打开摄像头并实时推理 |
| 停止预览 | `stop_preview` | 停止视频/摄像头预览 |
| 导出视频 | `export_video` | 处理完整视频并保存 |
| 截图当前帧 | `snapshot_current_frame` | 保存当前预览帧 |
| 开始录制 | `toggle_recording` | 开始或停止录制实时结果 |

### 3.3 GUI 加载模型流程

函数：

```python
VisualSegApp.load_model
```

所在文件：

```text
terminal/gui_app.py
```

核心代码：

```python
self.backend = YolactInferenceBackend(
    weights=weights,
    score_threshold=float(self.score_var.get()),
    disable_tensorrt=self.disable_tensorrt_var.get(),
)
```

处理流程：

```text
读取权重路径输入框
检查权重文件是否存在
读取阈值滑块
读取是否禁用 TensorRT
创建 YolactInferenceBackend 对象
```

从这一步开始，GUI 就拥有了一个推理后端：

```python
self.backend
```

后面处理图片、视频、摄像头都会调用它。

### 3.4 GUI 图片处理流程

函数：

```python
VisualSegApp.open_image
```

所在文件：

```text
terminal/gui_app.py
```

核心代码：

```python
original_bgr, result_bgr, combined = self.backend.process_image_file(path)
self.render_panels(original_bgr, result_bgr)
```

完整流程：

```text
用户点击“打开图片”
  ↓
open_image 被调用
  ↓
ensure_backend 确认模型已经加载
  ↓
filedialog.askopenfilename 弹出文件选择框
  ↓
用户选择图片路径 path
  ↓
self.backend.process_image_file(path)
  ↓
后端读取图片并推理
  ↓
返回 original_bgr、result_bgr、combined
  ↓
render_panels 把原图和结果图显示到界面
  ↓
状态栏显示已加载图片
```

这里的三个返回值含义是：

| 变量 | 含义 |
| --- | --- |
| `original_bgr` | 原始 OpenCV 图片 |
| `result_bgr` | 已经画好 mask、框、文字的结果图 |
| `combined` | 原图和结果图左右拼接后的图 |

### 3.5 GUI 图片显示流程

函数：

```python
VisualSegApp.render_panels
VisualSegApp.prepare_tk_image
```

所在文件：

```text
terminal/gui_app.py
```

核心代码：

```python
original_rgb = cv2.cvtColor(original_bgr, cv2.COLOR_BGR2RGB)
result_rgb = cv2.cvtColor(result_bgr, cv2.COLOR_BGR2RGB)

left_image = self.prepare_tk_image(original_rgb, 620, 620)
right_image = self.prepare_tk_image(result_rgb, 620, 620)

self.original_label.configure(image=left_image)
self.result_label.configure(image=right_image)
```

处理流程：

```text
OpenCV BGR 图像
  ↓
cv2.cvtColor 转成 RGB
  ↓
Image.fromarray 转成 Pillow Image
  ↓
thumbnail 缩放到界面可显示大小
  ↓
ImageTk.PhotoImage 转成 Tkinter 可显示对象
  ↓
Label.configure(image=...) 显示到界面
```

为什么要用 Pillow？

Tkinter 不能直接显示 OpenCV 的 `numpy.ndarray`，所以需要借助 Pillow 的 `Image` 和 `ImageTk.PhotoImage` 做一次转换。

### 3.6 GUI 视频处理流程

视频相关函数：

```python
VisualSegApp.open_video
VisualSegApp.play_video
VisualSegApp.start_preview_session
VisualSegApp.preview_loop
VisualSegApp.consume_preview_queue
```

所在文件：

```text
terminal/gui_app.py
```

处理流程：

```text
用户点击“打开视频”
  ↓
open_video 弹出文件选择框
  ↓
保存视频路径到 self.video_path
  ↓
用户点击“播放视频”
  ↓
play_video 创建 cv2.VideoCapture
  ↓
start_preview_session 启动后台线程
  ↓
preview_loop 在后台读取视频帧
  ↓
每一帧调用 self.backend.predict(frame)
  ↓
把结果放入 preview_queue
  ↓
consume_preview_queue 在主线程取出最新结果
  ↓
render_panels 更新界面显示
```

关键代码：

```python
capture = cv2.VideoCapture(self.video_path)
```

这句用 OpenCV 打开视频文件。

后台线程中：

```python
ok, frame = capture.read()
result_bgr, combined = self.backend.predict(frame)
```

这表示：

```text
从视频读取一帧
  ↓
把这一帧送入推理后端
  ↓
得到分割结果
```

### 3.7 GUI 摄像头处理流程

摄像头相关函数：

```python
VisualSegApp.start_camera
VisualSegApp.start_preview_session
VisualSegApp.preview_loop
VisualSegApp.consume_preview_queue
```

所在文件：

```text
terminal/gui_app.py
```

处理流程和视频几乎相同，区别是 `cv2.VideoCapture` 的输入不是文件路径，而是摄像头编号：

```python
capture = cv2.VideoCapture(camera_index)
```

例如：

```text
camera_index = 0
```

一般表示电脑默认摄像头。

摄像头流程：

```text
用户点击“启动摄像头”
  ↓
start_camera 读取摄像头索引
  ↓
cv2.VideoCapture(camera_index) 打开摄像头
  ↓
start_preview_session 启动后台线程
  ↓
preview_loop 不断读取摄像头画面
  ↓
每一帧调用 backend.predict
  ↓
consume_preview_queue 更新界面
```

### 3.8 为什么视频和摄像头要用线程

GUI 主线程负责响应按钮、刷新界面。如果直接在主线程里跑模型推理，界面会卡住。

所以本项目使用：

```python
self.preview_thread = threading.Thread(
    target=self.preview_loop,
    args=(session_id, capture, self.get_preview_max_side()),
    daemon=True,
)
```

也就是：

```text
后台线程负责读取帧和推理
主线程负责显示结果和响应用户操作
```

两者之间用队列传递结果：

```python
self.preview_queue = Queue(maxsize=1)
```

队列大小为 1 的意思是只保留最新一帧。如果推理速度跟不上视频速度，旧帧会被丢弃。这样可以减少延迟，让界面尽量显示最新画面。

## 4. 命令行前端处理流程

命令行前端文件：

```text
terminal/visual_terminal.py
```

它和 GUI 使用同一个后端：

```python
from inference_backend import YolactInferenceBackend
```

### 4.1 参数解析

函数：

```python
parse_args
```

所在文件：

```text
terminal/visual_terminal.py
```

它支持的关键参数包括：

| 参数 | 作用 |
| --- | --- |
| `--mode image` | 图片模式 |
| `--mode video` | 视频模式 |
| `--mode camera` | 摄像头模式 |
| `--input` | 图片或视频路径 |
| `--camera-index` | 摄像头索引 |
| `--weights` | 模型权重路径 |
| `--config` | 模型配置名 |
| `--dataset` | 数据集配置名 |
| `--score-threshold` | 置信度阈值 |
| `--top-k` | 最多显示多少个目标 |
| `--disable-tensorrt` | 禁用 TensorRT |
| `--output` | 导出路径 |
| `--save-side-by-side` | 是否保存原图和结果拼接图 |
| `--cpu` | 是否使用 CPU |

### 4.2 命令行入口流程

函数：

```python
main
```

所在文件：

```text
terminal/visual_terminal.py
```

流程：

```text
parse_args 读取命令行参数
  ↓
检查权重文件是否存在
  ↓
创建 YolactInferenceBackend
  ↓
根据 args.mode 选择图片、视频或摄像头流程
```

核心代码：

```python
backend = YolactInferenceBackend(
    weights=args.weights,
    config=args.config,
    dataset=args.dataset,
    score_threshold=args.score_threshold,
    top_k=args.top_k,
    disable_tensorrt=args.disable_tensorrt,
    use_cpu=args.cpu,
)
```

### 4.3 命令行图片模式

函数：

```python
run_image_mode
```

所在文件：

```text
terminal/visual_terminal.py
```

核心代码：

```python
_, result_bgr, combined = backend.process_image_file(args.input)
```

流程：

```text
读取输入图片
  ↓
调用后端 process_image_file
  ↓
得到分割结果
  ↓
如果指定 output，就保存结果
  ↓
用 cv2.imshow 显示窗口
  ↓
用户按 s 保存，按 q 或 Esc 退出
```

### 4.4 命令行视频/摄像头模式

函数：

```python
run_video_or_camera_mode
```

所在文件：

```text
terminal/visual_terminal.py
```

视频模式：

```python
cap = cv2.VideoCapture(str(args.input))
```

摄像头模式：

```python
cap = cv2.VideoCapture(args.camera_index)
```

循环处理每一帧：

```python
ok, frame = cap.read()
result_bgr, combined = backend.predict(frame)
cv2.imshow(WINDOW_NAME, display_frame)
```

完整流程：

```text
打开视频文件或摄像头
  ↓
循环读取一帧 frame
  ↓
backend.predict(frame) 推理
  ↓
fit_for_display 缩放显示图像
  ↓
cv2.imshow 显示结果
  ↓
如果正在录制，writer.write 写入视频
  ↓
读取键盘按键
```

按键功能：

| 按键 | 作用 |
| --- | --- |
| `q` 或 `Esc` | 退出 |
| `s` | 保存当前帧 |
| `r` | 开始或暂停录制 |

## 5. 推理后端初始化流程

推理后端文件：

```text
terminal/inference_backend.py
```

核心类：

```python
class YolactInferenceBackend
```

GUI 和命令行都会创建这个类。

### 5.1 创建运行参数

函数：

```python
build_runtime_args
```

所在文件：

```text
terminal/inference_backend.py
```

它把 GUI 或命令行传入的参数整理成一个对象，例如：

```python
trained_model=weights
score_threshold=score_threshold
top_k=top_k
disable_tensorrt=disable_tensorrt
cuda=cuda
cpu=use_cpu
```

这些参数会传给 YOLACT Edge 模型加载和推理过程。

### 5.2 初始化后端对象

函数：

```python
YolactInferenceBackend.__init__
```

所在文件：

```text
terminal/inference_backend.py
```

它主要做这些事：

```text
保存权重路径、配置名、数据集名
保存置信度阈值和 top_k
判断使用 CPU 还是 CUDA
创建 FastBaseTransform 预处理器
设置 mask 透明度
调用 _setup_model 加载模型
```

关键代码：

```python
self.transform = FastBaseTransform()
self._setup_model()
```

### 5.3 加载模型

函数：

```python
YolactInferenceBackend._setup_model
```

所在文件：

```text
terminal/inference_backend.py
```

核心代码：

```python
set_cfg(self.config)
set_dataset(self.dataset)

net = Yolact(training=False)
net.load_weights(self.weights, args=self.runtime_args)
net.eval()

self.net = net.to(self.device)
self.net.detect.use_fast_nms = True
```

流程：

```text
设置模型配置 yolact_edge_config
  ↓
设置数据集 coco2017_dataset
  ↓
关闭梯度计算
  ↓
创建 Yolact 模型
  ↓
加载 .pth 权重文件
  ↓
切换到 eval 推理模式
  ↓
如果启用 TensorRT，则进行 TensorRT 转换
  ↓
把模型移动到 CPU 或 GPU
  ↓
启用 fast_nms
```

这里的 `.eval()` 表示模型进入推理模式。推理模式和训练模式不同，它不会更新模型参数，也会让 BatchNorm 等层使用固定统计量。

## 6. 一张图如何进入模型

最核心函数：

```python
YolactInferenceBackend.predict
```

所在文件：

```text
terminal/inference_backend.py
```

这是整个项目最关键的函数之一。

### 6.1 输入

`predict` 的输入是：

```python
frame_bgr: np.ndarray
```

也就是一张 OpenCV 图像。

来源可能是：

```text
cv2.imread 读取的图片
cv2.VideoCapture.read 读取的视频帧
cv2.VideoCapture.read 读取的摄像头帧
```

### 6.2 可选缩小图像

函数：

```python
resize_for_inference
```

所在文件：

```text
terminal/inference_backend.py
```

代码位置在 `predict` 开头：

```python
working_frame = resize_for_inference(frame_bgr, inference_max_side)
```

作用：

```text
如果设置了预览最大边长，就把图像缩小后再推理
如果没有设置，就使用原图
```

这样做可以提高视频或摄像头预览速度。

### 6.3 把 OpenCV 图像转成 PyTorch Tensor

核心代码：

```python
frame_tensor = torch.from_numpy(working_frame).to(self.device).float()
```

所在函数：

```python
YolactInferenceBackend.predict
```

所在文件：

```text
terminal/inference_backend.py
```

这一步非常关键，可以拆成三部分理解：

```python
torch.from_numpy(working_frame)
```

把 NumPy 图像数组转成 PyTorch Tensor。

```python
.to(self.device)
```

把 Tensor 移动到 CPU 或 GPU。

```python
.float()
```

把数据类型转为浮点数，因为神经网络通常使用浮点数计算。

### 6.4 增加 batch 维度并预处理

核心代码：

```python
batch = self.transform(frame_tensor.unsqueeze(0))
```

所在函数：

```python
YolactInferenceBackend.predict
```

处理步骤：

```text
frame_tensor.unsqueeze(0)
  给单张图片增加 batch 维度
  ↓
self.transform(...)
  调用 FastBaseTransform 预处理
```

`FastBaseTransform` 文件：

```text
yolact_edge/yolact_edge/utils/augmentations.py
```

核心类：

```python
class FastBaseTransform(torch.nn.Module)
```

它的 `forward` 主要做：

```python
img = img.permute(0, 3, 1, 2).contiguous()
img = F.interpolate(img, (cfg.max_size, cfg.max_size), mode='bilinear', align_corners=False)
img = (img - self.mean) / self.std
img = img[:, (2, 1, 0), :, :].contiguous()
```

含义如下：

| 操作 | 作用 |
| --- | --- |
| `permute(0, 3, 1, 2)` | 把维度从 `[N, H, W, C]` 改成 `[N, C, H, W]` |
| `F.interpolate` | 把图像缩放到模型配置尺寸 |
| `(img - mean) / std` | 归一化，让输入数值分布适合模型 |
| `img[:, (2, 1, 0), :, :]` | 把 BGR 通道改成 RGB 通道 |

为什么要改维度？

OpenCV 图像习惯是：

```text
高度 H × 宽度 W × 通道 C
```

PyTorch 卷积神经网络习惯是：

```text
批次 N × 通道 C × 高度 H × 宽度 W
```

所以必须做维度调整。

### 6.5 构造 extras 参数

核心代码：

```python
extras = {
    "backbone": "full",
    "interrupt": False,
    "keep_statistics": False,
    "moving_statistics": None,
}
```

所在函数：

```python
YolactInferenceBackend.predict
```

`extras` 是传给 YOLACT Edge 模型的额外控制参数。

在本项目 GUI 默认流程中：

```text
backbone = full
```

表示每一帧都完整经过 backbone 主干网络。

### 6.6 调用模型推理

核心代码：

```python
preds = self.net(batch, extras=extras)["pred_outs"]
```

所在函数：

```python
YolactInferenceBackend.predict
```

这里的 `self.net` 是前面加载好的：

```python
net = Yolact(training=False)
```

对应文件：

```text
yolact_edge/yolact_edge/yolact.py
```

核心类：

```python
class Yolact(nn.Module)
```

这一步就是把预处理后的图片真正送进 YOLACT Edge 模型。

## 7. YOLACT Edge 模型内部流程

核心函数：

```python
Yolact.forward
```

所在文件：

```text
yolact_edge/yolact_edge/yolact.py
```

模型内部流程如下：

```text
输入 batch
  ↓
backbone 提取图像特征
  ↓
FPN 融合多尺度特征
  ↓
proto_net 生成 prototype masks
  ↓
prediction_layers 预测边框、类别、mask 系数
  ↓
Detect 解码边框并执行 NMS
  ↓
输出 pred_outs
```

### 7.1 backbone 提取特征

相关代码：

```python
outs = self.backbone(x)
```

所在函数：

```python
Yolact.forward
```

`backbone` 是主干网络，负责从图片中提取特征。

可以理解为：

```text
原始图片是像素
backbone 把像素转换成更抽象的特征
例如边缘、纹理、物体局部结构
```

### 7.2 FPN 多尺度特征融合

相关类：

```python
class FPN
```

所在文件：

```text
yolact_edge/yolact_edge/yolact.py
```

相关代码：

```python
outs = self.fpn(outs)
```

FPN 是 Feature Pyramid Network，中文可理解为“特征金字塔网络”。

作用：

```text
高分辨率特征适合检测小目标
低分辨率特征适合检测大目标
FPN 把不同尺度的特征融合起来
```

### 7.3 proto_net 生成 prototype masks

相关代码：

```python
proto_out = self.proto_net(proto_x)
proto_out = cfg.mask_proto_prototype_activation(proto_out)
proto_out = proto_out.permute(0, 2, 3, 1).contiguous()
```

所在函数：

```python
Yolact.forward
```

`proto_net` 会生成一组全图共享的 mask 原型。它们还不是某个具体物体的最终 mask，而是一组可组合的基础 mask。

### 7.4 prediction_layers 预测检测结果

相关类：

```python
class PredictionModule
```

所在文件：

```text
yolact_edge/yolact_edge/yolact.py
```

每个预测层输出：

```python
preds = { 'loc': bbox, 'conf': conf, 'mask': mask, 'priors': priors }
```

含义：

| 字段 | 含义 |
| --- | --- |
| `loc` | 边框位置预测 |
| `conf` | 类别置信度预测 |
| `mask` | mask 系数 |
| `priors` | 预设 anchor/prior 框 |

### 7.5 Detect 解码和 NMS

相关类：

```python
class Detect
```

所在文件：

```text
yolact_edge/yolact_edge/layers/functions/detection.py
```

调用位置：

```python
outs_wrapper["pred_outs"] = self.detect(pred_outs)
```

处理内容：

```text
decode 把边框预测转换成真实坐标
按置信度过滤低分框
执行 fast_nms 或 traditional_nms
保留最终检测结果
```

输出结果大致包含：

```python
{
    'box': boxes,
    'mask': masks,
    'class': classes,
    'score': scores,
    'proto': proto_data
}
```

## 8. 模型输出如何变成最终分割图

模型输出后，还不能直接显示，需要进一步后处理和绘制。

后处理函数：

```python
postprocess
```

所在文件：

```text
yolact_edge/yolact_edge/layers/output_utils.py
```

调用位置：

```python
processed = postprocess(
    dets_out,
    width,
    height,
    crop_masks=True,
    score_threshold=self.score_threshold,
)
```

所在函数：

```python
YolactInferenceBackend._draw_predictions
```

所在文件：

```text
terminal/inference_backend.py
```

### 8.1 postprocess 的输入

`postprocess` 接收：

| 参数 | 含义 |
| --- | --- |
| `det_output` | Detect 输出的检测结果 |
| `w` | 原图宽度 |
| `h` | 原图高度 |
| `crop_masks` | 是否把 mask 裁剪到检测框内 |
| `score_threshold` | 置信度阈值 |

### 8.2 postprocess 的主要处理

核心流程：

```text
根据 score_threshold 过滤低分检测
  ↓
取出 classes、boxes、scores、masks
  ↓
如果是 lincomb mask 类型：
      使用 proto_data @ masks.t() 组合出实例 mask
  ↓
对 mask 使用激活函数
  ↓
把 mask 裁剪到检测框内
  ↓
把 mask 上采样回原图大小
  ↓
把 mask 二值化
  ↓
把边框坐标转换为原图像素坐标
```

关键代码：

```python
masks = proto_data @ masks.t()
masks = cfg.mask_proto_mask_activation(masks)
masks = crop(masks, boxes)
masks = F.interpolate(masks.unsqueeze(0), (h, w), mode=interpolation_mode, align_corners=False).squeeze(0)
masks.gt_(0.5)
```

可以简单理解为：

```text
prototype masks
  ×
每个物体自己的 mask 系数
  =
每个物体自己的分割 mask
```

### 8.3 postprocess 的输出

返回值：

```python
return classes, scores, boxes, masks
```

含义：

| 返回值 | 含义 |
| --- | --- |
| `classes` | 每个检测目标的类别编号 |
| `scores` | 每个检测目标的置信度 |
| `boxes` | 每个检测目标的边框坐标 |
| `masks` | 每个检测目标的实例分割 mask |

## 9. 如何把分割结果画到图片上

绘制函数：

```python
YolactInferenceBackend._draw_predictions
```

所在文件：

```text
terminal/inference_backend.py
```

它负责把 `postprocess` 的结果画回图片上。

### 9.1 取出类别、分数、框、mask

核心代码：

```python
classes = processed[0][: self.top_k].detach().cpu().numpy()
scores = processed[1][: self.top_k].detach().cpu().numpy()
boxes = processed[2][: self.top_k].detach().cpu().numpy()
masks = processed[3][: self.top_k] if cfg.eval_mask_branch else None
```

这里的 `.detach().cpu().numpy()` 表示：

```text
从 PyTorch 计算图中分离
  ↓
移动到 CPU
  ↓
转换成 NumPy 数组
```

因为 OpenCV 绘图更适合使用 NumPy 数组。

### 9.2 绘制半透明 mask

核心代码：

```python
output = (
    output.astype(np.float32) * (1 - mask_3c * self.mask_alpha)
    + colored * (mask_3c * self.mask_alpha)
).astype(np.uint8)
```

这段是在做颜色混合：

```text
原图颜色 × 保留比例
  +
mask 颜色 × 透明度
```

这样画出来的 mask 是半透明的，既能看到分割区域，也能看到原图内容。

### 9.3 绘制边框

核心代码：

```python
cv2.rectangle(output, (x1, y1), (x2, y2), color, 2)
```

含义：

```text
在 output 图片上画一个矩形框
左上角坐标是 (x1, y1)
右下角坐标是 (x2, y2)
颜色是 color
线宽是 2
```

### 9.4 绘制类别文字

核心代码：

```python
class_name = cfg.dataset.class_names[int(classes[idx])]
label = f"{class_name}: {scores[idx]:.2f}"
cv2.putText(output, label, ...)
```

例如模型检测到人，置信度为 0.93，文字可能是：

```text
person: 0.93
```

### 9.5 返回结果图

最后：

```python
return output
```

这个 `output` 就是已经画好 mask、边框、类别文字的 OpenCV 图像。

## 10. 图片文件完整流程

以 GUI 打开图片为例，完整调用链如下：

```text
terminal/gui_app.py
VisualSegApp.open_image
  ↓
terminal/inference_backend.py
YolactInferenceBackend.process_image_file
  ↓
cv2.imread
  ↓
terminal/inference_backend.py
YolactInferenceBackend.predict
  ↓
resize_for_inference
  ↓
torch.from_numpy
  ↓
FastBaseTransform.forward
  ↓
yolact_edge/yolact_edge/yolact.py
Yolact.forward
  ↓
yolact_edge/yolact_edge/layers/functions/detection.py
Detect.__call__
  ↓
terminal/inference_backend.py
YolactInferenceBackend._draw_predictions
  ↓
yolact_edge/yolact_edge/layers/output_utils.py
postprocess
  ↓
cv2.rectangle / cv2.putText / mask 混合
  ↓
terminal/gui_app.py
VisualSegApp.render_panels
  ↓
Tkinter 界面显示结果
```

关键入口代码：

```python
original_bgr, result_bgr, combined = self.backend.process_image_file(path)
```

## 11. 视频文件完整流程

以 GUI 播放视频为例，完整调用链如下：

```text
terminal/gui_app.py
VisualSegApp.open_video
  ↓
保存视频路径 self.video_path
  ↓
VisualSegApp.play_video
  ↓
cv2.VideoCapture(self.video_path)
  ↓
VisualSegApp.start_preview_session
  ↓
启动后台线程 preview_thread
  ↓
VisualSegApp.preview_loop
  ↓
capture.read 读取一帧
  ↓
YolactInferenceBackend.predict(frame)
  ↓
模型推理和结果绘制
  ↓
push_preview_item 放入队列
  ↓
VisualSegApp.consume_preview_queue
  ↓
render_panels 显示最新帧
```

这里每一帧都会重复：

```text
读取帧 → 推理 → 绘制 → 显示
```

## 12. 摄像头完整流程

摄像头和视频类似，只是输入源不同：

```text
视频：cv2.VideoCapture(视频路径)
摄像头：cv2.VideoCapture(摄像头编号)
```

完整调用链：

```text
terminal/gui_app.py
VisualSegApp.start_camera
  ↓
cv2.VideoCapture(camera_index)
  ↓
VisualSegApp.start_preview_session
  ↓
后台线程 preview_loop
  ↓
capture.read 读取摄像头当前帧
  ↓
YolactInferenceBackend.predict(frame)
  ↓
模型推理和结果绘制
  ↓
consume_preview_queue 更新 GUI
```

## 13. 导出视频流程

GUI 导出视频函数：

```python
VisualSegApp.export_video
```

所在文件：

```text
terminal/gui_app.py
```

后端导出函数：

```python
YolactInferenceBackend.export_video
```

所在文件：

```text
terminal/inference_backend.py
```

后端核心流程：

```text
cv2.VideoCapture 打开输入视频
  ↓
读取 FPS、宽度、高度、总帧数
  ↓
cv2.VideoWriter 创建输出视频
  ↓
循环读取每一帧
  ↓
predict(frame) 推理当前帧
  ↓
writer.write 写入结果帧
  ↓
回调 progress_callback 更新 GUI 状态栏
  ↓
释放 cap 和 writer
```

关键代码：

```python
result_bgr, combined = self.predict(frame)
writer.write(combined if save_side_by_side else result_bgr)
```

如果用户勾选“导出拼接图”，就写入 `combined`；否则只写入 `result_bgr`。

## 14. 截图和录制流程

### 14.1 截图

函数：

```python
VisualSegApp.snapshot_current_frame
```

所在文件：

```text
terminal/gui_app.py
```

流程：

```text
检查当前是否有结果图
  ↓
生成 snapshot_时间.jpg 文件名
  ↓
根据是否勾选“导出拼接图”选择 result 或 combined
  ↓
cv2.imwrite 保存图片
```

### 14.2 实时录制

相关函数：

```python
VisualSegApp.toggle_recording
VisualSegApp.start_recording
VisualSegApp.write_recording_frame
VisualSegApp.stop_recording
```

所在文件：

```text
terminal/gui_app.py
```

流程：

```text
用户点击“开始录制”
  ↓
start_recording 创建 cv2.VideoWriter
  ↓
视频/摄像头每显示一帧
  ↓
write_recording_frame 写入当前结果帧
  ↓
用户停止或预览结束
  ↓
stop_recording 释放 writer
```

## 15. 保存图片流程

函数：

```python
VisualSegApp.save_current_image
```

所在文件：

```text
terminal/gui_app.py
```

流程：

```text
用户点击“保存图片”
  ↓
弹出保存路径选择框
  ↓
根据是否勾选“导出拼接图”选择保存内容
  ↓
ensure_parent 确保父目录存在
  ↓
cv2.imwrite 保存图片
```

相关工具函数：

```python
ensure_parent
```

所在文件：

```text
terminal/inference_backend.py
```

作用：

```text
确保输出文件所在的文件夹存在
```

## 16. 结果图 combined 是什么

函数：

```python
combine_views
```

所在文件：

```text
terminal/inference_backend.py
```

作用：

```text
把原图和结果图左右拼接
中间加一条分隔线
```

核心代码：

```python
separator = np.full((original_bgr.shape[0], 12, 3), 40, dtype=np.uint8)
return np.hstack([original_bgr, separator, result_bgr])
```

拼接结果：

```text
原始画面 | 分割结果
```

## 17. 置信度阈值和 top_k

### 17.1 置信度阈值

GUI 中的阈值滑块对应：

```python
self.score_var = tk.DoubleVar(value=0.3)
```

传入后端：

```python
score_threshold=float(self.score_var.get())
```

作用：

```text
低于该分数的检测结果不显示
```

例如阈值是 0.3：

```text
person: 0.93 显示
car: 0.21 不显示
```

### 17.2 top_k

`top_k` 表示最多显示多少个目标。

后端默认：

```python
top_k: int = 15
```

也就是最多显示 15 个检测目标。

## 18. TensorRT 在流程中的位置

TensorRT 是 NVIDIA 的推理加速工具，可以让模型在支持的 GPU 上跑得更快。

相关代码：

```python
if not self.disable_tensorrt:
    convert_to_tensorrt(net, cfg, self.runtime_args, transform=BaseTransform())
```

所在函数：

```python
YolactInferenceBackend._setup_model
```

所在文件：

```text
terminal/inference_backend.py
```

本项目 GUI 默认：

```python
self.disable_tensorrt_var = tk.BooleanVar(value=True)
```

也就是默认禁用 TensorRT，优先走纯 PyTorch 推理流程。

## 19. 最简理解版本

如果只记一条主线，可以这样理解：

```text
前端拿到图片
  ↓
OpenCV 把图片变成 numpy 数组
  ↓
torch.from_numpy 把 numpy 数组变成 PyTorch Tensor
  ↓
FastBaseTransform 把 Tensor 调整成模型需要的格式
  ↓
Yolact 模型输出检测框、类别、mask 系数和 prototype mask
  ↓
Detect 做过滤和 NMS
  ↓
postprocess 组合出每个物体的真实 mask
  ↓
OpenCV 把 mask、框、文字画到图上
  ↓
GUI 或命令行窗口显示结果
```

对应最核心的函数链：

```text
gui_app.py
  VisualSegApp.open_image / preview_loop

inference_backend.py
  YolactInferenceBackend.process_image_file
  YolactInferenceBackend.predict
  YolactInferenceBackend._draw_predictions

augmentations.py
  FastBaseTransform.forward

yolact.py
  Yolact.forward
  PredictionModule.forward

detection.py
  Detect.__call__
  Detect.detect
  Detect.fast_nms / Detect.traditional_nms

output_utils.py
  postprocess
```

