# 项目改动说明

本文档用于说明当前项目相对于上游 `WisconsinAIVision/yolact_edge` 的主要变化。上游项目定位是 YOLACT Edge 论文代码仓库，重点提供模型结构、训练、评估、TensorRT 加速、Docker 和 Jetson 部署支持；当前项目在保留算法主体的基础上，面向 Windows 本地演示场景做了工程化封装、可视化入口、依赖整理和兼容性调整。

## 一、总体变化

当前项目没有重写 YOLACT Edge 的核心模型结构，主干网络、FPN、mask prototype、prediction head、后处理、训练入口、评估入口和 TensorRT 转换逻辑仍主要沿用上游代码体系。

主要变化集中在以下几个方面：

1. 增加 Windows 本地演示能力。
2. 增加桌面 GUI 和终端可视化入口。
3. 增加统一推理后端，封装模型加载、CUDA/CPU、TensorRT 开关、图片/视频/摄像头推理。
4. 增加截图、录制、视频导出、原图与结果拼接等展示功能。
5. 整理 Windows 环境安装文档和中文说明。
6. 将 `cocoapi` 源码纳入项目，降低 Windows 下额外拉取依赖的成本。
7. 对 Cython、pycocotools、路径、编码、TensorRT 默认策略等做了本地兼容性处理。

## 二、项目结构变化

### 1. 新增 `terminal/` 本地演示目录

新增目录：

```text
terminal/
├── gui_app.py
├── inference_backend.py
├── visual_terminal.py
└── run_gui.bat
```

各文件作用如下：

- `terminal/gui_app.py`：桌面 GUI 主程序，基于 Tkinter，实现权重选择、模型加载、图片打开、视频预览、摄像头预览、截图、录制和导出。
- `terminal/inference_backend.py`：统一推理后端，负责加载 YOLACT Edge 模型、选择 CUDA/CPU、调用 TensorRT 转换、执行前处理/推理/后处理绘制。
- `terminal/visual_terminal.py`：简化命令行演示入口，支持 `image`、`video`、`camera` 三种模式。
- `terminal/run_gui.bat`：Windows 下的一键启动脚本，直接调用项目内虚拟环境运行 GUI。

这部分是当前项目区别于上游原项目的核心新增内容。上游主要通过 `eval.py` 和 `train.py` 使用模型；当前项目将原始算法能力包装成了更适合本地展示和课程/项目演示的应用入口。

### 2. 新增 `result/` 演示结果目录

新增了本地演示素材：

```text
result/
├── main.jpg
├── test.jpg
├── test.gif
├── camera.png
├── camera.jpg
├── camera.gif
├── camera.mp4
└── 999.mp4
```

该目录主要用于 README 展示和项目效果说明，不参与算法逻辑。

### 3. 新增并整理中文文档

新增或重写了面向 Windows 本地运行的中文说明：

- 根目录 `README.md`
- `yolact_edge/README_CN.md`
- `yolact_edge/INSTALL.md`
- `yolact_edge/配置说明.md`
- `项目流程.md`
- 本文档 `项目改动.md`

这些文档重点说明本地环境、PyTorch CUDA、TensorRT 开关、权重路径、图片/视频/摄像头演示流程。

### 4. 纳入 `cocoapi/`

当前项目将 `cocoapi` 源码放入仓库，包含 Python、Matlab、Lua 等接口文件。实际本地 Windows 推理主要依赖其中的 PythonAPI，用于安装 `pycocotools`。

这样做的原因是上游安装流程通常要求用户自行拉取或安装 COCO API，而 Windows 下 `pycocotools` 的编译经常受 MSVC、NumPy 头文件、编译参数影响。将源码放入仓库后，安装路径更明确，也便于统一说明。

## 三、功能变化

### 1. 图片推理演示

上游原项目已支持：

```powershell
python eval.py --trained_model=weights/yolact_edge_54_800000.pth --image=test.jpg
```

当前项目新增了两种更直接的使用方式：

```powershell
python terminal/visual_terminal.py --mode image --input yolact_edge/test.jpg --disable-tensorrt
```

或通过 GUI 打开图片并显示原图与分割结果。

### 2. 视频推理和导出

当前项目在封装层中增加了：

- 视频逐帧预览。
- 视频结果导出。
- 原图与结果图拼接导出。
- 默认输出目录 `terminal/outputs/`。

对应实现主要在 `terminal/inference_backend.py` 的 `export_video` 和 `terminal/visual_terminal.py` 的视频模式中。

### 3. 摄像头实时演示

上游原项目可通过 `--video=0` 方式调用摄像头，但属于原生命令行参数能力。当前项目将其封装为 GUI 按钮和终端 `camera` 模式：

```powershell
python terminal/visual_terminal.py --mode camera --camera-index 0 --disable-tensorrt
```

GUI 中还支持摄像头截图和录制。

### 4. 统一后端封装

新增 `YolactInferenceBackend` 类，将以下逻辑集中管理：

- 设置配置 `set_cfg` / `set_dataset`。
- 加载权重。
- 判断 CUDA 是否可用。
- 设置 PyTorch 默认 tensor 类型。
- 可选 TensorRT 转换。
- 执行 `FastBaseTransform` 前处理。
- 调用 `net(batch, extras=...)` 推理。
- 调用 `postprocess` 后处理。
- 绘制 mask、bbox、类别和分数。
- 返回单张结果图或拼接图。

这让 GUI 和终端入口不需要直接处理上游 `eval.py` 中的大量参数和分支逻辑。

## 四、代码兼容性调整

本节是当前项目最重要的工程调整部分。上游项目偏 Linux / Jetson / Docker 环境，当前项目为了在 Windows + Python 3.8 + PyTorch CUDA 环境下运行，做了以下兼容性处理。

### 1. `setup.py` 增加 NumPy 头文件路径

文件：

```text
yolact_edge/setup.py
```

当前项目的 `setup.py` 中为 `cython_nms` 扩展增加了：

```python
include_dirs=[np.get_include()]
```

作用是让 Windows 编译 Cython 扩展时能找到 NumPy 头文件，例如：

```text
numpy/arrayobject.h
```

如果没有这个配置，Windows 下执行 `pip install .` 或编译 `cython_nms.pyx` 时容易出现 NumPy 头文件找不到的问题。

同时保留：

```python
cmdclass.update({'build_ext': build_ext})
ext_modules = [Extension("cython_nms", ["yolact_edge/utils/cython_nms.pyx"], include_dirs=[np.get_include()])]
```

这使 `yolact_edge` 可以作为 Python 包安装，并编译项目内部的 NMS Cython 扩展。

### 2. 调整 `pyximport` 的 reload 行为

文件：

```text
yolact_edge/yolact_edge/layers/functions/detection.py
```

当前项目将：

```python
pyximport.install(setup_args={"include_dirs":np.get_include()}, reload_support=True)
```

调整为：

```python
pyximport.install(setup_args={"include_dirs": np.get_include()}, reload_support=False)
```

这个变化很小，但对 Windows 运行比较关键。`reload_support=True` 可能导致 Cython 扩展在运行时生成带临时后缀的动态库，并触发重复编译、文件占用、加载异常等问题。Windows 对 `.pyd` 文件锁定更严格，运行中重复加载或覆盖扩展模块更容易失败。

关闭 reload support 后，运行路径更稳定，更适合本地演示项目：扩展模块只按普通方式加载，不在运行期间频繁尝试 reload。

### 3. `cocoapi/PythonAPI/setup.py` 适配 Windows 编译

文件：

```text
cocoapi/PythonAPI/setup.py
```

当前项目中的 `pycocotools` 编译配置使用：

```python
extra_compile_args=[]
```

并保留：

```python
include_dirs = [np.get_include(), '../common']
```

这样做是为了避免 Linux/GCC 专用编译参数影响 MSVC 编译。许多 COCO API 源码版本会带有类似 `-Wno-cpp`、`-Wno-unused-function` 等 GCC 参数，这些参数在 Windows MSVC 下不兼容，容易导致安装失败。

当前配置更适合本地 Windows 环境：

- 使用 NumPy 头文件路径。
- 使用 `../common` 中的 `maskApi.c`。
- 不附加 GCC 专用参数。

### 4. 清理已编译的 pycocotools 构建产物

在新增可视化前端的提交中，删除了部分已编译产物和 egg-info 文件，例如：

```text
cocoapi/PythonAPI/build/
cocoapi/PythonAPI/pycocotools.egg-info/
*.pyd
*.obj
*.lib
*.exp
```

这些文件与本机 Python 版本、平台 ABI、编译器版本强绑定。将它们保留在仓库中会导致其他机器安装时出现不一致或误加载问题。

清理后，用户应在自己的环境中重新编译安装 `pycocotools`。

### 5. 增加根目录 `.gitignore`

当前项目新增根目录 `.gitignore`，忽略：

```text
__pycache__/
*.py[cod]
*.pyd
.venv*/
build/
dist/
*.egg-info/
terminal/outputs/
yolact_edge/results/
yolact_edge/build/
yolact_edge/.venv38/
yolact_edge/weights/
cocoapi/PythonAPI/build/
cocoapi/PythonAPI/pycocotools.egg-info/
```

这属于工程兼容性和仓库卫生调整。Windows 下 Python 编译、Cython 编译和 GUI 导出会产生大量本地文件，如果不忽略，会污染提交记录。

需要注意：历史提交中曾加入过 `.venv38` 的部分文件，但当前 `.gitignore` 已经将其列为应忽略内容。后续不应继续提交虚拟环境和本机编译产物。

### 6. Windows 路径和入口封装

上游 `eval.py` 的 `--image` 参数使用类似：

```text
input.jpg:output.jpg
```

这种写法在 Windows 绝对路径中容易和盘符冲突，例如：

```text
C:\input.jpg:D:\output.jpg
```

当前项目通过 GUI 和 `terminal/visual_terminal.py` 封装输入输出路径，减少用户直接拼接 `:` 参数的机会。文档中也明确建议使用相对路径，或将输入文件放入项目目录内。

### 7. TensorRT 默认策略调整

上游项目强调 TensorRT 加速，默认配置中 `yolact_edge_config` 会启用部分 `torch2trt` 转换项，包括 INT8 相关转换。

当前 Windows 本地演示封装中，默认倾向于禁用 TensorRT：

```python
disable_tensorrt: bool = True
```

GUI 中也默认：

```python
self.disable_tensorrt_var = tk.BooleanVar(value=True)
```

原因是 Windows 下 TensorRT 依赖链更复杂，需要同时匹配：

- NVIDIA Driver
- CUDA
- cuDNN
- TensorRT
- PyTorch CUDA 版本
- torch2trt
- Visual Studio C++ 编译工具

而且 INT8 TensorRT 还需要校准图片。当前项目的目标是优先完成本地演示，因此默认推荐：

```text
PyTorch CUDA + --disable_tensorrt
```

在图片、视频、摄像头、导出全部跑通后，再单独启用 TensorRT 做性能优化。

### 8. CUDA/CPU 选择逻辑封装

`terminal/inference_backend.py` 中新增了运行参数构造逻辑：

```python
cuda = not use_cpu and torch.cuda.is_available()
```

并根据设备设置：

```python
self.device = torch.device(
    "cpu" if self.runtime_args.cpu or not torch.cuda.is_available() else "cuda"
)
```

这使本地演示程序能够自动判断当前机器是否有可用 CUDA。如果 CUDA 可用，模型和输入张量走 GPU；如果用户显式指定 CPU 或 CUDA 不可用，则退回 CPU。

这比直接使用上游 `eval.py` 参数更适合 GUI 场景。

### 9. 图像显示和导出兼容性处理

新增后端处理了 OpenCV 和 Tkinter/Pillow 的图像格式差异：

- OpenCV 使用 BGR。
- Tkinter/Pillow 显示需要 RGB。
- 模型输出结果需要再转回可显示或可保存的图像。

GUI 中通过：

```python
cv2.cvtColor(original_bgr, cv2.COLOR_BGR2RGB)
cv2.cvtColor(result_bgr, cv2.COLOR_BGR2RGB)
```

进行显示转换。

视频导出中使用：

```python
cv2.VideoWriter_fourcc(*"mp4v")
```

以提高 Windows 本地保存 MP4 的可用性。

### 10. 摄像头和视频流释放处理

GUI 和终端入口都增加了对 `VideoCapture` / `VideoWriter` 的释放逻辑：

```python
cap.release()
writer.release()
cv2.destroyAllWindows()
```

GUI 中还使用线程和队列处理预览帧，避免视频/摄像头推理长时间阻塞界面。

这些都是面向本地桌面应用的工程化补充，上游算法仓库没有这类 GUI 状态管理需求。

## 五、保留的上游能力

当前项目仍保留上游 YOLACT Edge 的主要能力：

- `yolact_edge/eval.py`：原生评估和推理入口。
- `yolact_edge/train.py`：训练入口。
- `yolact_edge/yolact_edge/yolact.py`：模型主体。
- `yolact_edge/yolact_edge/data/config.py`：配置系统。
- `yolact_edge/yolact_edge/utils/tensorrt.py`：TensorRT 转换逻辑。
- `yolact_edge/docker/`：Docker 和 Jetson 相关文件。
- `yolact_edge/data/scripts/`：数据集脚本。

也就是说，当前项目是在上游算法基础上增加本地应用封装，而不是只保留了演示代码。

## 六、当前项目运行路线

当前项目推荐的本地运行路线是：

1. 使用 Python 3.8 创建虚拟环境。
2. 安装 PyTorch CUDA 版本，例如 `torch==1.8.1+cu111`。
3. 安装 OpenCV、Pillow、NumPy、Cython、matplotlib 等基础依赖。
4. 从 `cocoapi/PythonAPI` 安装 `pycocotools`。
5. 从 `yolact_edge` 目录执行 `pip install .`，编译 `cython_nms`。
6. 使用 `--disable_tensorrt` 跑通基础 PyTorch CUDA 推理。
7. 再使用 GUI 或终端封装进行图片、视频、摄像头演示。
8. 如需加速，再额外配置 TensorRT / torch2trt。

这一路线降低了首次运行难度，避免把 TensorRT 环境问题和基础推理问题混在一起。

## 七、相对上游的价值总结

当前项目相对原 YOLACT Edge 仓库的主要价值是：

- 把论文算法代码转成可演示、可交互的 Windows 本地应用。
- 降低使用门槛，减少必须理解原始 `eval.py` 大量参数的成本。
- 将图片、视频、摄像头三个常用演示场景统一封装。
- 增加本地结果导出、截图、录制、拼接图等展示能力。
- 对 Windows 编译、Cython、pycocotools、路径、TensorRT 默认策略做了实用兼容性处理。
- 保留原算法结构和训练/评估能力，方便后续继续做模型优化或二次开发。

简而言之，当前项目是一个基于 YOLACT Edge 的 Windows 本地实例分割演示工程，核心算法来自上游，主要新增工作集中在工程封装、交互展示和本地兼容性上。
